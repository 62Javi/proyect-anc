version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8003:8000"
    volumes:
      - ./backend:/app
    environment:
      - ENV=dev

  frontend:
    build: ./frontend
    ports:
      - "8083:80"
    depends_on:
      - backend
